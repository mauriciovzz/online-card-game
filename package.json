{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.9.5"
  },
  "scripts": {
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "lint-staged": {
    "**/*.{ts,tsx,js,jsx,json,css,mjs}": [
      "prettier --write"
    ]
  }
}
